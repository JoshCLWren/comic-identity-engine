#!/usr/bin/env bash
# direnv configuration for Comic Identity Engine

# Load uv virtual environment
layout uv

# Ensure Python and pytest are available from uv
export PATH=$(dirname $(uv which python)):$PATH
